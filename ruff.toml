line-length = 100
target-version = "py39"

[lint]
select = ["E", "F", "W", "I"]   # errors, pyflakes, warnings, imports
ignore = ["E501"]                 # ignore line-too-long (handled by formatter)
